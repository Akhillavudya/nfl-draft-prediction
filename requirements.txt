# Core dependencies (pinned to the versions in the working venv).
# Mirrors pyproject.toml. Install with: pip install -r requirements.txt
lightgbm==4.6.0
catboost==1.2.10
optuna==4.9.0
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
joblib==1.5.3

# Explainability (Phase C): SHAP values + the plotting backend we save figures with.
shap==0.51.0
matplotlib==3.10.9

# Experiment tracking (Phase C): W&B dashboard + .env loader for the API key.
wandb==0.28.0
python-dotenv==1.2.2

# Serverless deployment (Phase D): Modal SDK + CLI (the `modal` command).
modal==1.5.1

# Dev/test only.
pytest==9.1.1
